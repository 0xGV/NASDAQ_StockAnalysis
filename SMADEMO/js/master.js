/**
 * ============================================================================
 * master.js -- Handles site onload workload
 * ============================================================================
 */

function master() { 
    navByEnter();       // first, enable hitting enter to search
    linkme();           // then, enable linking to a specific tab
}